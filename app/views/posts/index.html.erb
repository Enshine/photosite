<%- if current_user %>
	<% @posts.each do |p| %>
	
		<div>
			<%= image_tag p.image.url(:medium)%>
			<br>
			<%= p.description %>
		</div>
	
		<% end %>
	<% else %>
		<p> 
			<h2> Hello! Wellcome to photosite! </h2>
		</p>
<% end %>

